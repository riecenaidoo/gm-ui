/**
 * TODO [design] perhaps take in the route for use as breadcrumbs in the future once we have true nested structure
 *  such as User > Profile > Catalogue > Playlist.
 */
export interface Page {
  title: string;
}
